apiVersion: 1

providers:
  - name: "sync-batch-dashboards"
    orgId: 1
    folder: "Sync Batch"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
